# Extra nextflow params for this study, applied by bin/run_study.sh before any
# args passed on the command line (so a command-line arg can still override one
# of these). One flag/value per line; '#' starts a comment; blank lines ignored.

# diamond over phmmer/blast for the pairwise search: this study's real
# structure (484 IN x 543 targets = 262,812 pairs, see the mmseqs quick-look
# analysis in results/quick_look/) needs the fast option -- same reasoning
# pezizo_set1's run_params.txt already documents.
--run_tool diamond
